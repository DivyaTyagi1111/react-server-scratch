import React from "react";
// import ImageCardCSS from "./ImageCard.module.css";
// import { parseImageUrl } from "../../Utils";
const ImageCard = (props) => {
    return(
        // <div className={ImageCardCSS['img-div']} style = {{'height': `${props.imgData.imgHeight}`,'padding': `${props.imgData.padding}`}}>
        //     <img className={ImageCardCSS['img']} src={parseImageUrl(props.imgData.aspectRatio,props.imgData.imgUrl,props.imgData.imgWidth)[0]}></img>
        // </div>
        <h1>Image Card</h1>
    );
}

export default ImageCard;